[package]
name = "oksidizasyon"
version = "0.1.0"
edition = "2024"

[features]
default = ["std"]
std = []
emd = ["dep:embedded-alloc"]
#std = ["alloc"]
#alloc = []

[dependencies]
libm = "0.2"
#heapless = "0.9.3"
embedded-alloc = { version = "0.7.0", optional = true }